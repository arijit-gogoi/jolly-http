# Copy this file to `.env` and fill in real values.
# Run with --env-file .env --require-env .env.example to fail-fast on missing keys.

# Base URL of the API under test.
API_BASE=

# Bearer token for authenticated requests.
API_TOKEN=
